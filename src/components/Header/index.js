import React from 'react';
import PersonIcon from '@mui/icons-material/Person';

const Header = () => {
  return (
    <div>
      {' '}
      header
      <PersonIcon />
    </div>
  );
};

export default Header;
